server:
  port: 8080

# 配置 spring 项目的名字
spring:
  application:
    name: oj-system
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/spring_oj_dev?useUnicode=true&characterEncoding=utf8&useSSL=true&serverTimezone=GMT%2B8
    username: zmbdp
    password: 123456

  hikari:
    minimum-idle: 5                   # 最小空闲连接数
    maximum-pool-size: 20             # 最大连接数
    idle-timeout: 30000               # 空闲连接存活时间（毫秒）
    connection-timeout: 30000         # 连接超时时间（毫秒）
    # connection-test-query: SELECT 1  # 如果驱动支持 JDBC 4.0，建议不设置；默认使用 Connection.isValid() 来检查连接活性（更高效）
    # keepalive-time: 10000            # 检查连接活性的时间间隔，默认不启用
